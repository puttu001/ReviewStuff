# Backend base URL
VITE_API_URL=http://localhost:8000

# Google OAuth Client ID (public — it ships in the JS bundle by design).
# The Client Secret is NOT used by this app and must never be put here.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
